# Internationalisierung (i18n)

Diese Dokumentation beschreibt das Internationalisierungssystem der Radiolyze Anwendung.

## Übersicht

Die Anwendung verwendet [react-i18next](https://react.i18next.com/) für die Mehrsprachunterstützung. Aktuell werden folgende Sprachen unterstützt:

| Sprache | Code | Status |
|---------|------|--------|
| Deutsch | `de` | ✅ Vollständig |
| Englisch | `en` | ✅ Vollständig |

## Dateistruktur

```
src/i18n/
├── index.ts                    # i18n Konfiguration
└── locales/
    ├── de/                     # Deutsche Übersetzungen
    │   ├── common.json         # Allgemeine UI-Texte
    │   ├── report.json         # Befund-bezogene Texte
    │   ├── viewer.json         # DICOM Viewer Texte
    │   ├── batch.json          # Batch-Reporting Texte
    │   ├── settings.json       # Einstellungen
    │   ├── errors.json         # Fehlermeldungen
    │   └── training.json       # Seite „Training Data Export“
    └── en/                     # Englische Übersetzungen
        ├── common.json
        ├── report.json
        ├── viewer.json
        ├── batch.json
        ├── settings.json
        ├── errors.json
        └── training.json
```

## Namespaces

Die Übersetzungen sind in logische Namespaces unterteilt:

| Namespace | Verwendung |
|-----------|------------|
| `common` | Navigation, Buttons, Status, allgemeine Labels |
| `report` | Befund-Editor, Impressions, QA, Templates |
| `viewer` | DICOM Viewer, Vergleichsmodus, Sync-Optionen |
| `batch` | Batch-Reporting Dashboard, Statistiken |
| `settings` | Benutzereinstellungen, Konfiguration |
| `errors` | Fehlermeldungen, Validierung |
| `training` | Seite „Training Data Export“ |

---

## Für Entwickler

### Verwendung in Komponenten

```tsx
import { useTranslation } from 'react-i18next';

export function MyComponent() {
  const { t } = useTranslation('common'); // Namespace angeben
  
  return (
    <div>
      <h1>{t('navigation.home')}</h1>
      <button>{t('actions.save')}</button>
    </div>
  );
}
```

### Mehrere Namespaces

```tsx
const { t } = useTranslation(['report', 'common']);

// Namespace explizit angeben
t('report:findings.title')
t('common:actions.save')
```

### Interpolation (Variablen)

```tsx
// JSON: "wordCount": "{{count}} Wörter"
t('findings.wordCount', { count: 150 })

// JSON: "frame": "Bild {{current}} von {{total}}"
t('progress.frame', { current: 5, total: 20 })
```

### Pluralisierung

```tsx
// JSON:
// "study": "{{count}} Studie",
// "study_plural": "{{count}} Studien"
t('study', { count: 1 })  // → "1 Studie"
t('study', { count: 5 })  // → "5 Studien"
```

### Dynamische Schlüssel

```tsx
const status = 'approved'; // pending | inProgress | draft | approved
t(`status.${status}`)      // → "Freigegeben"
```

Ein dynamischer Schlüssel ist ein Vertrag mit der Quelle des Werts. Diesen mit
einem Test festhalten, der jeden möglichen Wert durchgeht — so schlägt ein neu
hinzugekommener Wert im Test fehl, statt den rohen Schlüssel anzuzeigen. Siehe
`src/i18n/__tests__/resources.test.ts` für die Drift-Metriken und die
Export-Formate.

### Label-Tabellen außerhalb von Komponenten

Eine Tabelle mit Anzeigenamen in einem Modul — `src/types/*.ts`, eine
`config/`-Konstante, ein `FORMAT_INFO` auf Modulebene — wird einmal beim Import
ausgewertet. Dort abgelegte Strings sind auf die Sprache festgelegt, in der sie
geschrieben wurden, und folgen keinem Sprachwechsel. Ein `useTranslation`-Handle
hilft nicht: Das Modul ist keine Komponente und hat kein Rendering, das erneut
laufen könnte.

Die Daten bleiben im Modul, die Wörter gehören in die Resources:

```ts
// src/types/annotations.ts — nur die Member, kein Anzeigetext
export const ANNOTATION_CATEGORY_VALUES: AnnotationCategory[] = ["nodule", "mass", /* … */];
```

```tsx
// an der Render-Stelle aufgelöst — ein Sprachwechsel formatiert neu, was auf dem Schirm steht
{ANNOTATION_CATEGORY_VALUES.map((value) => (
  <SelectItem key={value} value={value}>{t(`annotations.categories.${value}`)}</SelectItem>
))}
```

Damit wird die Member-Liste zu einem dynamischen Schlüssel und braucht den oben
beschriebenen Contract-Test — `src/i18n/__tests__/resources.test.ts` läuft über
alle Member der Annotations-, MPR- und VRT-Tabellen.

### Markup innerhalb eines Satzes

`<Trans>` verwenden, wenn ein Teil eines Satzes ein eigenes Element braucht. Den
Satz stattdessen in Präfix- und Suffix-Schlüssel zu zerlegen zwingt jede
Übersetzung in die deutsche Wortstellung.

```tsx
// JSON: "zipHint": "Das Export-ZIP enthält ein Manifest unter <code>images/manifest.json</code>."
<Trans t={t} i18nKey="export.zipHint" components={{ code: <code /> }} />
```

Die Tag-Namen im String werden gegen die Keys von `components` aufgelöst; ein
Name ohne Entsprechung landet als Text im DOM. Ein Test auf den Resource-String
allein sieht das nicht — daher mit einem Render-Test absichern, der das Element
im DOM findet, wie in `src/pages/__tests__/Training.test.tsx`.

### Außerhalb von React (Class Components, Utilities)

```tsx
import i18n from '@/i18n';

// Direkte Verwendung
const label = i18n.t('common:actions.save');

// Sprache wechseln
i18n.changeLanguage('en');
```

### Neue Übersetzungsschlüssel hinzufügen

1. **Schlüssel in allen Sprachdateien hinzufügen:**

```json
// src/i18n/locales/de/common.json
{
  "myFeature": {
    "title": "Mein Feature",
    "description": "Beschreibung hier"
  }
}

// src/i18n/locales/en/common.json
{
  "myFeature": {
    "title": "My Feature",
    "description": "Description here"
  }
}
```

2. **In Komponente verwenden:**

```tsx
const { t } = useTranslation('common');
return <h1>{t('myFeature.title')}</h1>;
```

### Der ESLint-Guard

`i18next/no-literal-string` läuft als **Warnung** über `src/components/**`
(`eslint.config.js`). Der Grund: Der Bestand an hartkodierten Strings ist
zwischen den Aufräumrunden schneller nachgewachsen, als die Sweeps ihn abgebaut
haben.

Die Regel prüft nur JSX-Text, `className`, `id` und `style` werden also nicht
gemeldet. Kinder von `<kbd>` und `<code>` sind ausgenommen — Tastennamen (`Esc`,
`Shift+LMB`) und Bezeichner werden in keiner Sprache übersetzt — ebenso Kinder
von `<Trans>`, die das Markup eines bereits übersetzten Schlüssels sind.

Eine Warnung hier ist ein Fund, kein Rauschen: Der String gehört in den
Namespace der jeweiligen Oberfläche. Soll ein String tatsächlich nicht übersetzt
werden — ein Produktname, ein Einheitenzeichen —, dann per Inline-Disable mit
Begründung, statt die Warnung stehen zu lassen.

Die Regel blockiert noch nicht und gilt noch nicht außerhalb von
`src/components/**`; `src/config/viewer.ts` und Teile von `src/pages/**` tragen
weiterhin unübersetzte Tabellen. Danach kann sie verschärft werden.

### Best Practices für Entwickler

- ✅ **Immer alle Sprachen aktualisieren** – Nie nur eine Sprachdatei ändern
- ✅ **Semantische Schlüssel verwenden** – `patient.age` statt `label1`
- ✅ **Gruppierung nutzen** – Zusammengehörige Texte in Objekten
- ✅ **Interpolation für dynamische Werte** – Keine String-Konkatenation
- ✅ **Namespace passend wählen** – Feature-spezifische Texte richtig einordnen
- ❌ **Keine hartcodierten Strings** – Alle UI-Texte müssen übersetzt sein
- ❌ **Keine HTML in Übersetzungen** – Markup in Komponenten halten

---

## Für Übersetzer

### JSON-Format

Übersetzungsdateien sind im JSON-Format:

```json
{
  "sectionName": {
    "key": "Übersetzter Text",
    "keyWithVariable": "Hallo {{name}}!",
    "nestedSection": {
      "deepKey": "Tief verschachtelter Text"
    }
  }
}
```

### Wichtige Regeln

1. **Schlüssel nicht ändern** – Nur die Werte (rechte Seite) übersetzen
2. **Variablen beibehalten** – `{{variable}}` nicht übersetzen oder entfernen
3. **Struktur beibehalten** – Verschachtelung und Kommas beachten
4. **Kontext beachten** – Medizinische Fachbegriffe korrekt verwenden

### Beispiel-Übersetzung

```json
// Original (Deutsch)
{
  "findings": {
    "title": "Befund",
    "wordCount": "{{count}} Wörter"
  }
}

// Übersetzung (Englisch)
{
  "findings": {
    "title": "Findings",
    "wordCount": "{{count}} words"
  }
}
```

### Medizinische Terminologie

| Deutsch | Englisch | Kontext |
|---------|----------|---------|
| Befund | Findings | Radiologischer Befundtext |
| Beurteilung | Impression | Zusammenfassung/Diagnose |
| Voruntersuchung | Prior Study | Frühere Bildgebung |
| Modalität | Modality | CT, MRT, Röntgen, etc. |
| Freigabe | Approval | Befundautorisierung |
| Diktat | Dictation | Spracherkennung |

### Validierung

Vor dem Einreichen prüfen:

- [ ] JSON-Syntax gültig (keine fehlenden Kommas/Klammern)
- [ ] Alle Variablen `{{...}}` vorhanden
- [ ] Keine leeren Werte
- [ ] Fachbegriffe korrekt verwendet
- [ ] Konsistente Terminologie innerhalb der Datei

---

## Neue Sprache hinzufügen

### 1. Ordner und Dateien erstellen

```bash
mkdir src/i18n/locales/fr
cp src/i18n/locales/en/*.json src/i18n/locales/fr/
```

### 2. Dateien übersetzen

Alle kopierten JSON-Dateien ins Französische übersetzen.

### 3. In i18n/index.ts registrieren

```typescript
// Imports hinzufügen
import frCommon from './locales/fr/common.json';
import frReport from './locales/fr/report.json';
// ... weitere Imports

// In resources ergänzen
export const resources = {
  de: { /* ... */ },
  en: { /* ... */ },
  fr: {
    common: frCommon,
    report: frReport,
    viewer: frViewer,
    batch: frBatch,
    settings: frSettings,
    errors: frErrors,
  },
} as const;
```

### 4. Sprachauswahl in Settings erweitern

In `src/pages/Settings.tsx` die neue Sprache zur Auswahl hinzufügen.

---

## Sprachauswahl

Die Sprache wird in folgender Reihenfolge bestimmt:

1. **Benutzereinstellung** – Gespeichert in localStorage (`medgemma-user-preferences`)
2. **Browser-Sprache** – Automatische Erkennung
3. **Fallback** – Deutsch (`de`)

### Programmatisch wechseln

```typescript
import i18n from '@/i18n';
import { useUserPreferences } from '@/hooks/useUserPreferences';

// In Komponente
const { updatePreferences } = useUserPreferences();

const handleLanguageChange = (lang: 'de' | 'en') => {
  i18n.changeLanguage(lang);
  updatePreferences({ uiLanguage: lang });
};
```

---

## Debugging

### Fehlende Übersetzungen finden

Im Development-Modus (`import.meta.env.DEV`) werden fehlende Schlüssel in der Konsole geloggt.

### Übersetzung testen

```typescript
// In Browser-Konsole
i18next.t('common:actions.save')
i18next.changeLanguage('en')
```

---

## Checkliste für neue Features

- [ ] Alle UI-Strings in passende Namespace-Datei aufnehmen
- [ ] Übersetzungen für alle unterstützten Sprachen hinzufügen
- [ ] `useTranslation` Hook in Komponente verwenden
- [ ] Interpolation für dynamische Werte nutzen
- [ ] Manuelle Tests in beiden Sprachen durchführen
