fastapi
uvicorn[standard]
httpx
pydantic
# pydicom-seg pulls SegmentationStorage from a pre-3.0 import path; pin
# to the 2.x line until pydicom-seg ships a 3.0-compatible release.
pydicom>=2.4,<3.0
numpy<3
SimpleITK>=2.5.5
scikit-image>=0.26.0
Pillow>=12.3.0
trimesh>=4.12.2
pygltflib>=1.16.5
pydicom-seg>=0.4.1
# --- M2: TotalSegmentator (multi-organ) ---
# torch is pulled transitively but pinned here so docker rebuilds do not
# silently switch the CUDA build channel out from under us.
torch>=2.12.1
totalsegmentator>=2.15.0
