fastapi
uvicorn[standard]
httpx
pydantic
pydicom>=3.0.2,<4
numpy<3
SimpleITK>=2.5.6
scikit-image>=0.26.0
Pillow>=12.3.0
trimesh>=5.0.0
# Quadric decimation backend for trimesh. NOT optional here despite being an
# optional trimesh extra: `Trimesh.simplify_quadric_decimation` is a thin
# wrapper around it and raises ModuleNotFoundError without it, which
# app/meshing.py catches and turns into an un-decimated mesh. Removing this
# line silently disables MESH_MAX_FACES.
fast-simplification>=0.2.0
pygltflib>=1.16.5
# DICOM SEG writer (app/dicom_seg.py). Replaced pydicom-seg, which is
# unmaintained and never gained a pydicom 3 release.
highdicom>=0.28.1
# --- M2: TotalSegmentator (multi-organ) ---
# torch arrives transitively via totalsegmentator; it is named here only to
# hold a floor, NOT to pin. This is a lower bound, so two rebuilds a month
# apart can still land on different torch releases and different CUDA build
# channels. If reproducible segmenter images become a requirement, this needs
# a real pin (== plus a hash-locked constraints file), not a >=.
torch>=2.13.0
totalsegmentator>=2.18.0
